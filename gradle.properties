org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# GitHub Packages credentials for karoo-ext dependency.
# Add your GitHub username and a Personal Access Token (with read:packages scope) here,
# or set GPR_USER and GPR_KEY environment variables instead.
# gpr.user=YOUR_GITHUB_USERNAME
# gpr.key=YOUR_GITHUB_PAT
